// app/layout.tsx - Updated
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Globe.GL Gallery - Interactive 3D Data Visualizations",
  description: "Explore stunning interactive 3D globe visualizations powered by Globe.GL. Gaming revenue, global remittances, firearms data, and more.",
  keywords: "globe visualization, 3D data, interactive maps, Globe.GL, data visualization, global statistics",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
